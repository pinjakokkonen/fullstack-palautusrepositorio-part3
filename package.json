{
  "name": "fullstack-palautusrepositorio-part3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../fullstack-palautusrepositorio/osa2/puhelinluettelo/ && npm run build && cp -r dist ../fullstack-palautusrepositorio-part3/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "author": "Pinja",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
